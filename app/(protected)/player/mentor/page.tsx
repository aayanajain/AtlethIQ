// app/(protected)/player/mentor/page.tsx
//
// AI mentor — makeshift placeholder. This will become the conversational
// coach/mentor chat for the player (Tier 2 in CLAUDE.md). It sits inside the
// player sidebar shell (layout.tsx).

export default function PlayerMentorPage() {
  return (
    <div className="flex min-h-screen items-center justify-center p-8">
      <div className="text-center">
        <div className="text-4xl">🚧</div>
        <h1 className="mt-3 text-2xl font-bold text-zinc-900 dark:text-zinc-50">
          AI mentor
        </h1>
        <p className="mt-1 text-zinc-500">Under development</p>
      </div>
    </div>
  );
}
